# Adaptive-tick reconciler plane — the controller-manager
# equivalent: the store-driven workload reconcilers as cooperating PIC modules on
# ONE relaxable domain. This is the realistic unit that benefits from adaptive
# cadence, as opposed to the apiserver front door (linux_net/tls/api_ingress),
# which is socket-driven and stays hot — see fluxor-controlplane.yaml.
#
# Every module here declares timer_class = "agnostic" (a pure store transform,
# cadence-independent), so the whole group clears the mechanism-(b) attestation
# gate in `validate_adaptive_tick`. The node/kubelet liveness modules
# (pod_lifecycle, probe_runner) are timer_class = "tick_counted" — they count
# ticks as time for probe periods, so they are DELIBERATELY excluded: fluxor's
# build gate would reject this graph outright if one were added here (proven by
# the negative control fluxor-reconcilers-adaptive-bad.yaml).
#
# Domain `main` enables adaptive tick:
#   adaptive_flags = 3 = IDLE (bit 0) | CADENCE (bit 1)
#   tick_min_us = 1000    hard floor — the responsive base tick, never crossed
#   tick_max_us = 50000   idle backstop — the pacer walks up to here when the
#                         plane has no pending store change; any SUBSCRIBE push
#                         latches an event wake that snaps back to the floor.
# Net: an idle controller-manager parks ~20x/s instead of ~1000x/s, with no
# added latency on real reconcile work.
target: linux
tick_us: 1000

# Every reconciler self-wires status -> changes (a 1-node cycle) to allocate its
# store-change sink channel.
scheduler:
  accept_cycles: true

execution:
  domains:
    - name: main
      cores: [0]
      adaptive_flags: 3
      tick_min_us: 1000
      tick_max_us: 50000

modules:
  # The scheduler is PARAMS: the FIRST unbound Pod (where spec.nodeName=,
  # count index 0) JOIN /ready-nodes/ picked by the fewest Pods bound to it —
  # measured from the Pods themselves at pick time — PATCHes spec.nodeName;
  # one bind per pass, the spread emergent. Then the Scheduled event. Reads
  # /ready-nodes/ from the DaemonSet chain's projection. Params from
  # modules/app/_chronicle/scheduler.uproc.
  - name: sc_unbound
    type: store_source
    prefix: "/pods/"
    where: "spec.nodeName="
    count: 1
    join: "/ready-nodes/"
    join_pick: "mincount:/pods/:spec.nodeName"
    watch: "/ready-nodes/"
  - name: sc_bind
    type: decision
    params:
      # chronicle-source: modules/app/_chronicle/scheduler.uproc bd
      decision: ""
  - name: sc_patch
    type: store_effect
    key_prefix: "/pods/"
  - name: sc_record
    type: decision
    params:
      # chronicle-source: modules/app/_chronicle/scheduler.uproc rc
      decision: ""
  - name: sc_events
    type: store_effect
    key_prefix: "/events/"
  # the ready-nodes projection (the DaemonSet chain's rdy decision)
  - name: ds_nodes
    type: store_source
    prefix: "/nodes/"
    paths: "status.ready"
  - name: ds_ready
    type: decision
    params:
      # chronicle-source: modules/app/_chronicle/daemonset.uproc rdy
      decision: ""
  - name: ds_mark
    type: store_effect
    key_prefix: "/ready-nodes/"
    domain: main
  - name: deployment_reconciler
    domain: main
  - name: replicaset_reconciler
    domain: main
  - name: daemonset_reconciler
    domain: main
  - name: statefulset_reconciler
    domain: main
  - name: job_reconciler
    domain: main
  - name: hpa_reconciler
    domain: main
  - name: garbage_collector
    domain: main
  - name: namespace_gc
    domain: main

wiring:
  - from: sc_unbound.status
    to: sc_unbound.changes
  - from: sc_unbound.record_out
    to: sc_bind.record_in
  - from: sc_bind.result_out
    to: sc_patch.request_in
    buffer_group: 111
  - from: sc_patch.response_out
    to: sc_record.record_in
  - from: sc_record.result_out
    to: sc_events.request_in
    buffer_group: 112
  - from: ds_nodes.status
    to: ds_nodes.changes
  - from: ds_nodes.record_out
    to: ds_ready.record_in
  - from: ds_ready.result_out
    to: ds_mark.request_in
    buffer_group: 41
  - from: deployment_reconciler.status
    to: deployment_reconciler.changes
  - from: replicaset_reconciler.status
    to: replicaset_reconciler.changes
  - from: daemonset_reconciler.status
    to: daemonset_reconciler.changes
  - from: statefulset_reconciler.status
    to: statefulset_reconciler.changes
  - from: job_reconciler.status
    to: job_reconciler.changes
  - from: hpa_reconciler.status
    to: hpa_reconciler.changes
  - from: garbage_collector.status
    to: garbage_collector.changes
  - from: namespace_gc.status
    to: namespace_gc.changes
